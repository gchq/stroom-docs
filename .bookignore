# GitBook ignore file.
# We ignore everything that isn't docs
.gitignore
.bookignore
_unused/*

# Some of our images are generated at build time so are in gitignore
# This exception will override .puml.svg files being ignored by gitignore
!*.svg
